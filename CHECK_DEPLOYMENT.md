# Проверка деплоя после обновления

## Описание

Скрипты для автоматической проверки деплоя после обновления Railway. Проверяют работоспособность всех endpoints и генерацию тестовой структуры.

## Использование

### Вариант 1: Полная проверка (рекомендуется)

```bash
./check_deployment.sh [BASE_URL]
```

**Пример:**
```bash
./check_deployment.sh https://iva-production-4400.up.railway.app
```

**Что проверяет:**
1. ✅ Healthcheck endpoint (`/health/`)
2. ✅ API Status endpoint (`/api/status/`)
3. ✅ Structure endpoint (`/api/structure/`)
4. ✅ Генерация структуры (`/api/structure/generate/`)
5. ✅ Структура содержит данные
6. ✅ Structure tree endpoint (`/api/structure/tree/`)
7. ✅ Дерево содержит данные
8. ✅ Statistics endpoint (`/api/stats/`)
9. ✅ Tariffs endpoint (`/api/tariffs/`)
10. ✅ Telegram мини-приложение (`/telegram-app/`)

### Вариант 2: Быстрая проверка

```bash
./check_deployment_quick.sh [BASE_URL]
```

**Пример:**
```bash
./check_deployment_quick.sh https://iva-production-4400.up.railway.app
```

**Что проверяет:**
1. ✅ Healthcheck endpoint
2. ✅ Генерация структуры
3. ✅ Проверка дерева структуры

## Требования

- `curl` (должен быть установлен)
- Доступ к интернету
- Правильный BASE_URL (по умолчанию `https://iva-production-4400.up.railway.app`)

## Установка

Скрипты уже исполняемые, но если нужно:

```bash
chmod +x check_deployment.sh
chmod +x check_deployment_quick.sh
```

## Примеры вывода

### Успешная проверка

```
🔍 Проверка деплоя на https://iva-production-4400.up.railway.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Проверка Healthcheck
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Проверка: Healthcheck endpoint... ✅ OK (200)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. Проверка API Status
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Проверка: API Status... ✅ OK
   Ответ: {"name":"Equilibrium API","version":"0.1.0","status":"ok"}...

...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Итоговый результат
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Все проверки пройдены успешно!

🎉 Деплой завершен успешно!
🔗 Мини-приложение: https://iva-production-4400.up.railway.app/telegram-app/
🔗 API структуры: https://iva-production-4400.up.railway.app/api/structure/tree/
🔗 Статистика: https://iva-production-4400.up.railway.app/api/stats/
```

### Ошибка проверки

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Итоговый результат
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Обнаружено 1 ошибок

⚠️  Пожалуйста, проверьте логи Railway:
   1. Откройте Railway Dashboard
   2. Перейдите в проект backend-django
   3. Откройте сервис IVA
   4. Проверьте вкладку Logs
```

## Ручная проверка

Если скрипты не работают, можно проверить вручную:

### 1. Healthcheck
```bash
curl https://iva-production-4400.up.railway.app/health/
```

### 2. Генерация структуры
```bash
curl https://iva-production-4400.up.railway.app/api/structure/generate/?children=6
```

### 3. Проверка дерева
```bash
curl https://iva-production-4400.up.railway.app/api/structure/tree/
```

### 4. Статистика
```bash
curl https://iva-production-4400.up.railway.app/api/stats/
```

## Устранение проблем

### Ошибка: "Connection refused"

**Причина:** Сервер еще не запущен или недоступен.

**Решение:**
1. Подождите несколько минут после деплоя
2. Проверьте статус деплоя в Railway Dashboard
3. Убедитесь, что сервис активен

### Ошибка: "404 Not Found"

**Причина:** Endpoint не найден или URL неправильный.

**Решение:**
1. Проверьте правильность BASE_URL
2. Убедитесь, что изменения задеплоены
3. Проверьте логи Railway на наличие ошибок

### Ошибка: "500 Internal Server Error"

**Причина:** Ошибка на сервере.

**Решение:**
1. Проверьте логи Railway
2. Убедитесь, что база данных подключена
3. Проверьте переменные окружения

### Ошибка: "Структура пуста"

**Причина:** Структура не была создана.

**Решение:**
1. Вызовите endpoint `/api/structure/generate/?children=6`
2. Проверьте, что генерация прошла успешно
3. Проверьте логи Railway на наличие ошибок

## Интеграция с CI/CD

Скрипты можно использовать в CI/CD pipeline:

```yaml
# Пример для GitHub Actions
- name: Check Deployment
  run: |
    ./check_deployment.sh https://iva-production-4400.up.railway.app
```

## Автоматизация

Можно добавить проверку в `package.json` или `Makefile`:

```json
{
  "scripts": {
    "check-deployment": "./check_deployment.sh",
    "check-deployment-quick": "./check_deployment_quick.sh"
  }
}
```

Или в `Makefile`:

```makefile
check-deployment:
	./check_deployment.sh

check-deployment-quick:
	./check_deployment_quick.sh
```

## Дополнительная информация

- **Полная проверка:** Занимает ~10-15 секунд, проверяет все endpoints
- **Быстрая проверка:** Занимает ~3-5 секунд, проверяет только основные endpoints
- **Рекомендация:** Используйте полную проверку после деплоя, быструю - для периодических проверок
