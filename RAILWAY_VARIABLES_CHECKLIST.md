# Чеклист переменных окружения для Railway

## Backend сервис (iva-production.up.railway.app)

### ✅ Уже есть (из скриншота):
- `DATABASE_URL` ✅
- `TELEGRAM_BOT_TOKEN` ✅
- `TELEGRAM_WEBAPP_URL` ✅

### ⚠️ Нужно добавить:

#### Обязательные для работы:
1. **`RAILWAY_PUBLIC_DOMAIN`**
   - Значение: `iva-production.up.railway.app`
   - Для: Автоматическая настройка webhook и других URL

2. **`CORS_ALLOWED_ORIGINS`**
   - Значение: `https://iva.up.railway.app`
   - Для: Разрешить запросы с фронтенда

#### Рекомендуемые:
3. **`TELEGRAM_WEBHOOK_URL`**
   - Значение: `https://iva-production.up.railway.app/telegram/webhook/`
   - Для: Настройка Telegram webhook (автоматически, но лучше указать явно)

4. **`DJANGO_SECRET_KEY`**
   - Значение: Сгенерируйте случайную строку (минимум 50 символов)
   - Для: Безопасность Django (критично для production!)
   - ⚠️ **ВАЖНО**: Если не установлен, используется небезопасный default

5. **`DJANGO_DEBUG`**
   - Значение: `False`
   - Для: Production режим (безопасность)

6. **`DJANGO_ALLOWED_HOSTS`** (опционально)
   - Значение: `iva-production.up.railway.app,iva.up.railway.app`
   - Для: Дополнительная безопасность

#### Опциональные (если нужна кастомизация):
7. **`MAX_PARTNERS_PER_LEVEL`**
   - Значение: `3` (по умолчанию)
   - Для: Максимум партнеров на уровень в MLM структуре

8. **`DEFAULT_GREEN_BONUS_PERCENT`**
   - Значение: `50` (по умолчанию)
   - Для: Процент зеленого бонуса

9. **`DEFAULT_YELLOW_BONUS_PERCENT`**
   - Значение: `50` (по умолчанию)
   - Для: Процент желтого бонуса

---

## Frontend сервис (iva.up.railway.app)

### ⚠️ Нужно добавить:

1. **`VITE_API_URL`**
   - Значение: `https://iva-production.up.railway.app`
   - Для: Подключение к Backend API
   - ⚠️ **ОБЯЗАТЕЛЬНО** для работы фронтенда!

---

## Приоритет добавления

### 🔴 Критично (добавить немедленно):
1. `DJANGO_SECRET_KEY` - безопасность!
2. `VITE_API_URL` - для работы фронтенда
3. `RAILWAY_PUBLIC_DOMAIN` - для webhook

### 🟡 Важно (добавить в ближайшее время):
4. `CORS_ALLOWED_ORIGINS` - для работы фронтенда с бекендом
5. `TELEGRAM_WEBHOOK_URL` - для работы Telegram бота
6. `DJANGO_DEBUG=False` - production режим

### 🟢 Опционально (можно добавить позже):
7. `DJANGO_ALLOWED_HOSTS` - дополнительная безопасность
8. Остальные MLM настройки (если нужна кастомизация)

---

## Как добавить переменные на Railway

1. Откройте сервис на Railway
2. Перейдите в **Variables**
3. Нажмите **Add Variable**
4. Введите имя и значение
5. Нажмите **Add**
6. **Перезапустите сервис** после добавления переменных

---

## Генерация DJANGO_SECRET_KEY

Для генерации безопасного секретного ключа:

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

Или используйте онлайн генератор:
- https://djecrety.ir/
- https://miniwebtool.com/django-secret-key-generator/

---

## Проверка после добавления

После добавления всех переменных:

1. ✅ Перезапустите Backend сервис
2. ✅ Перезапустите Frontend сервис
3. ✅ Проверьте логи на ошибки
4. ✅ Откройте `https://iva.up.railway.app` - должен работать
5. ✅ Проверьте API: `https://iva-production.up.railway.app/api/status/`
6. ✅ Протестируйте Telegram бота: `/start`, `/app`, `/stats`

