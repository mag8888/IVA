# Быстрая настройка на Railway

## Текущая ситуация

✅ PostgreSQL сервис работает  
❌ Backend сервис (IVA) упал - не может подключиться к БД

## Что нужно сделать:

### Шаг 1: Подключить PostgreSQL к Backend сервису

1. **Откройте Backend сервис "IVA"** на Railway
2. Перейдите на вкладку **"Variables"**
3. Найдите кнопку **"New Variable"** или **"Reference Variable"**
4. Выберите **"Reference Variable"** (если доступно)
5. Выберите сервис **"Postgres"**
6. Выберите переменную **"DATABASE_URL"** или **"POSTGRES_URL"**
7. Railway автоматически добавит переменную

**ИЛИ** (если Reference Variable недоступно):

1. Откройте **Postgres** сервис
2. Перейдите на вкладку **"Variables"**
3. Найдите и скопируйте значение **"DATABASE_URL"** или **"POSTGRES_URL"**
4. Откройте **Backend сервис "IVA"**
5. Перейдите на вкладку **"Variables"**
6. Нажмите **"New Variable"**
7. Имя: `DATABASE_URL`
8. Значение: вставьте скопированный URL
9. Нажмите **"Add"**

### Шаг 2: Проверить другие переменные

Убедитесь, что в Backend сервисе есть:

- ✅ `DATABASE_URL` - подключение к PostgreSQL (ОБЯЗАТЕЛЬНО)
- ✅ `TELEGRAM_BOT_TOKEN` - токен бота (если используете бота)
- ✅ `NODE_ENV=production` - режим работы
- ✅ `PORT` - Railway установит автоматически

### Шаг 3: Перезапустить Backend

После добавления `DATABASE_URL`:

1. Railway автоматически перезапустит сервис
2. Или нажмите **"Redeploy"** вручную
3. Дождитесь завершения деплоя

### Шаг 4: Проверить логи

После перезапуска проверьте логи Backend сервиса. Должно быть:

```
🔍 Checking environment variables...
   DATABASE_URL: ✅ Set
🔄 Attempting to connect to database... (1/5)
✅ Connected to PostgreSQL database
✅ Database connection verified
✅ Database tables initialized
```

### Шаг 5: Проверить таблицы

После успешного запуска:

1. Откройте **Postgres** сервис
2. Перейдите на вкладку **"Database"** → **"Data"**
3. Должны появиться таблицы:
   - `users`
   - `messages`

## Если проблема сохраняется

### Проверьте:

1. ✅ PostgreSQL сервис запущен (зеленая галочка)
2. ✅ `DATABASE_URL` добавлен в Backend сервис
3. ✅ Формат `DATABASE_URL` правильный (начинается с `postgresql://`)
4. ✅ Backend и PostgreSQL в одном проекте Railway

### Типичные ошибки:

- ❌ `DATABASE_URL` не установлен → добавьте переменную
- ❌ Неправильный формат URL → скопируйте из Postgres сервиса
- ❌ Сервисы в разных проектах → переместите в один проект

## Настройка Frontend (позже)

После того как Backend заработает:

1. Создайте Frontend сервис
2. Укажите Root Directory: `frontend`
3. Добавьте переменную:
   - `NEXT_PUBLIC_API_URL` = URL вашего Backend сервиса
   - Например: `https://iva-production-xxxx.up.railway.app`

## Итого

**Сейчас нужно:**
1. ✅ Подключить `DATABASE_URL` к Backend сервису "IVA"
2. ✅ Перезапустить Backend
3. ✅ Проверить, что таблицы создались

**Frontend настраивать позже**, после того как Backend заработает.

