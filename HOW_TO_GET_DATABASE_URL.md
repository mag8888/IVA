# Как получить DATABASE_URL на Railway

## Способ 1: Автоматическое подключение (Самый простой) ⭐

### Шаги:

1. **Откройте Backend сервис "IVA"** на Railway
2. Перейдите на вкладку **"Variables"** (слева в меню)
3. Найдите кнопку **"+ New"** или **"Add Variable"**
4. Нажмите на **"Reference Variable"** (если доступно)
5. В выпадающем списке выберите:
   - **Service**: `Postgres` (ваш PostgreSQL сервис)
   - **Variable**: `DATABASE_URL` или `POSTGRES_URL`
6. Нажмите **"Add"** или **"Save"**

Railway автоматически подключит переменную и она будет обновляться автоматически!

## Способ 2: Ручное копирование

Если "Reference Variable" недоступно, скопируйте вручную:

### Шаг 1: Получить DATABASE_URL из PostgreSQL

1. **Откройте сервис "Postgres"** на Railway (в левом меню)
2. Перейдите на вкладку **"Variables"** (слева в меню)
3. Найдите переменную **"DATABASE_URL"** или **"POSTGRES_URL"**
4. Нажмите на значок **глаза** 👁️ или **копирования** 📋 рядом с переменной
5. **Скопируйте значение**

Пример того, как выглядит DATABASE_URL:
```
postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway
```

### Шаг 2: Добавить в Backend сервис

1. **Откройте сервис "IVA"** (Backend) на Railway
2. Перейдите на вкладку **"Variables"**
3. Нажмите **"+ New"** или **"New Variable"**
4. Заполните:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение
5. Нажмите **"Add"** или **"Save"**

## Способ 3: Через настройки подключения

1. **Откройте сервис "IVA"** (Backend)
2. Перейдите на вкладку **"Settings"**
3. Найдите секцию **"Connected Services"** или **"Service Connections"**
4. Нажмите **"Connect Service"** или **"Add Connection"**
5. Выберите **"Postgres"** сервис
6. Railway автоматически добавит `DATABASE_URL`

## Где искать DATABASE_URL в PostgreSQL сервисе

### Вариант 1: Вкладка Variables
- Откройте **Postgres** → **Variables**
- Ищите: `DATABASE_URL`, `POSTGRES_URL`, или `PGDATABASE`

### Вариант 2: Вкладка Database → Credentials
- Откройте **Postgres** → **Database** → **Credentials**
- Там может быть информация о подключении

### Вариант 3: В настройках подключения
- Откройте **Postgres** → **Settings**
- Может быть секция с connection string

## Формат DATABASE_URL

Правильный формат выглядит так:
```
postgresql://username:password@hostname:port/database
```

Пример:
```
postgresql://postgres:AbCdEf123@containers-us-west-123.railway.app:5432/railway
```

## Проверка

После добавления `DATABASE_URL`:

1. Перезапустите Backend сервис (Railway сделает это автоматически)
2. Проверьте логи Backend сервиса
3. Должно появиться:
   ```
   🔍 Checking environment variables...
      DATABASE_URL: ✅ Set
   🔄 Attempting to connect to database...
   ✅ Connected to PostgreSQL database
   ```

## Если не можете найти DATABASE_URL

### Альтернативные имена переменных:
- `DATABASE_URL`
- `POSTGRES_URL`
- `PGDATABASE`
- `POSTGRES_CONNECTION_STRING`

### Если переменной нет:
1. Railway может генерировать её автоматически
2. Попробуйте способ 1 (Reference Variable) - Railway сам найдет нужную переменную
3. Или создайте новый PostgreSQL сервис - Railway автоматически создаст DATABASE_URL

## Важно!

- ✅ `DATABASE_URL` должен начинаться с `postgresql://`
- ✅ Не изменяйте значение вручную (кроме как через Reference Variable)
- ✅ После добавления Railway автоматически перезапустит сервис
- ✅ Проверьте логи после перезапуска

## Визуальная подсказка

В Railway интерфейсе:
```
Postgres сервис
  └─ Variables
      └─ DATABASE_URL ← СКОПИРУЙТЕ ЭТО

IVA сервис (Backend)
  └─ Variables
      └─ + New Variable
          └─ Name: DATABASE_URL
          └─ Value: [вставьте скопированное]
```

