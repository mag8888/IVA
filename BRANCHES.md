# Структура веток проекта

## Ветки

### 🌐 `main` - Frontend ветка
- Используется для деплоя **Frontend сервиса** на Railway
- Содержит весь код проекта (backend + frontend)
- Root Directory для Railway: `frontend`

### ⚙️ `main_back` - Backend ветка
- Используется для деплоя **Backend сервиса** на Railway
- Содержит весь код проекта (backend + frontend)
- Root Directory для Railway: `backend`

## Использование на Railway

### Frontend Service:
- **Git Branch**: `main`
- **Root Directory**: `frontend`
- Railway будет деплоить из ветки `main`, но работать с файлами из папки `frontend/`

### Backend Service:
- **Git Branch**: `main_back`
- **Root Directory**: `backend`
- Railway будет деплоить из ветки `main_back`, но работать с файлами из папки `backend/`

## Работа с ветками

### Переключение между ветками:

```bash
# Переключиться на frontend ветку
git checkout main

# Переключиться на backend ветку
git checkout main_back
```

### Синхронизация изменений:

Если вы внесли изменения в одну ветку и хотите синхронизировать с другой:

```bash
# Например, изменили что-то в main и хотите в main_back
git checkout main
# ... делаете изменения ...
git add .
git commit -m "Изменения"
git push origin main

# Синхронизируем с main_back
git checkout main_back
git merge main
git push origin main_back
```

### Рекомендации:

1. **Обычно работайте в `main`** - это основная ветка
2. **Синхронизируйте `main_back`** после важных изменений
3. **Или работайте в обеих ветках** независимо, если нужны разные версии

## Настройка на Railway

### Frontend Service Settings:
```
Repository: mag8888/IVA
Branch: main              ← Ветка для фронтенда
Root Directory: frontend  ← Папка с фронтендом
```

### Backend Service Settings:
```
Repository: mag8888/IVA
Branch: main_back         ← Ветка для бэкенда
Root Directory: backend   ← Папка с бэкендом
```

## Важно

- Обе ветки содержат **весь код проекта**
- Разделение происходит через **Root Directory** в Railway
- Ветки нужны для независимого деплоя и версионирования
- Можно использовать одну ветку для обоих сервисов, но разделение дает больше контроля

