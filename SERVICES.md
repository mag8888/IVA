# Разделение Backend и Frontend сервисов

## 🎯 Главный принцип разделения

**Root Directory** - это ключевой параметр, который говорит Railway, где искать файлы для каждого сервиса.

## 📁 Структура файлов для Railway

```
Equilibrium/
│
├── backend/                    ← BACKEND СЕРВИС
│   ├── railway.json           ✅ Конфигурация Railway
│   ├── package.json           ✅ Имя: "equilibrium-backend"
│   ├── Procfile               ✅ Команда запуска
│   ├── nixpacks.toml          ✅ Конфигурация сборки
│   └── src/                   ✅ Исходный код
│
├── frontend/                   ← FRONTEND СЕРВИС
│   ├── railway.json           ✅ Конфигурация Railway
│   ├── package.json           ✅ Имя: "equilibrium-frontend"
│   ├── Procfile               ✅ Команда запуска
│   ├── nixpacks.toml          ✅ Конфигурация сборки
│   └── src/                   ✅ Исходный код
│
└── railway.json                ⚠️ Общий (не используется для разделения)
```

## 🔑 Маркеры для Railway

### Backend сервис определяется по:

1. **Root Directory = `backend`** ⭐ ГЛАВНОЕ
2. `backend/package.json` → `"name": "equilibrium-backend"`
3. `backend/railway.json` → конфигурация сборки и запуска
4. `backend/Procfile` → команда `web: npm run start`
5. `backend/nixpacks.toml` → настройки сборки

### Frontend сервис определяется по:

1. **Root Directory = `frontend`** ⭐ ГЛАВНОЕ
2. `frontend/package.json` → `"name": "equilibrium-frontend"`
3. `frontend/railway.json` → конфигурация сборки и запуска
4. `frontend/Procfile` → команда `web: npm run start`
5. `frontend/nixpacks.toml` → настройки сборки

## 🚀 Настройка на Railway

### Backend Service:

```
┌─────────────────────────────────────┐
│  Service Settings                   │
├─────────────────────────────────────┤
│  Service Name: backend              │
│  Root Directory: backend    ⭐     │
│  Build Command: (auto)              │
│  Start Command: (auto)              │
└─────────────────────────────────────┘
```

Railway будет:
1. Искать файлы в `backend/`
2. Читать `backend/package.json`
3. Использовать `backend/railway.json`
4. Запускать `npm run start` из `backend/`

### Frontend Service:

```
┌─────────────────────────────────────┐
│  Service Settings                   │
├─────────────────────────────────────┤
│  Service Name: frontend             │
│  Root Directory: frontend   ⭐     │
│  Build Command: (auto)              │
│  Start Command: (auto)              │
└─────────────────────────────────────┘
```

Railway будет:
1. Искать файлы в `frontend/`
2. Читать `frontend/package.json`
3. Использовать `frontend/railway.json`
4. Запускать `npm run start` из `frontend/`

## 📋 Чеклист настройки

### ✅ Backend сервис готов, если:

- [ ] Root Directory установлен в `backend`
- [ ] Есть `backend/package.json` с именем `equilibrium-backend`
- [ ] Есть `backend/railway.json`
- [ ] Есть `backend/Procfile`
- [ ] Переменные окружения:
  - [ ] `DATABASE_URL`
  - [ ] `TELEGRAM_BOT_TOKEN`
  - [ ] `NODE_ENV=production`

### ✅ Frontend сервис готов, если:

- [ ] Root Directory установлен в `frontend`
- [ ] Есть `frontend/package.json` с именем `equilibrium-frontend`
- [ ] Есть `frontend/railway.json`
- [ ] Есть `frontend/Procfile`
- [ ] Переменные окружения:
  - [ ] `NEXT_PUBLIC_API_URL` (URL backend сервиса)
  - [ ] `NODE_ENV=production`

## 🔍 Как Railway понимает тип сервиса

### Алгоритм определения:

```
1. Railway получает Root Directory
   ↓
2. Переходит в указанную папку (backend/ или frontend/)
   ↓
3. Ищет package.json
   ↓
4. Читает имя пакета и скрипты
   ↓
5. Ищет railway.json для конфигурации
   ↓
6. Ищет Procfile для команды запуска
   ↓
7. Собирает и запускает сервис
```

### Пример для Backend:

```
Root Directory: "backend"
  → Railway: "Ищу файлы в backend/"
  → Находит: backend/package.json
  → Видит: "name": "equilibrium-backend"
  → Понимает: "Это backend сервер"
  → Читает: backend/railway.json
  → Запускает: npm run start (из backend/)
```

### Пример для Frontend:

```
Root Directory: "frontend"
  → Railway: "Ищу файлы в frontend/"
  → Находит: frontend/package.json
  → Видит: "name": "equilibrium-frontend"
  → Понимает: "Это frontend сервер"
  → Читает: frontend/railway.json
  → Запускает: npm run start (из frontend/)
```

## 💡 Важные замечания

1. **Root Directory - это ВСЁ!** Без правильного Root Directory Railway не найдет нужные файлы.

2. **Один репозиторий, два сервиса**: Оба сервиса могут использовать один репозиторий, но с разными Root Directory.

3. **Автоматическое определение**: Railway автоматически определяет тип проекта по:
   - Наличию `package.json` с Next.js → Frontend
   - Наличию `package.json` с Express → Backend
   - Но Root Directory - это приоритет!

4. **Переменные окружения**: Каждый сервис имеет свой набор переменных окружения.

## 🛠️ Команды для проверки

После настройки проверьте логи:

### Backend:
```bash
# В Railway UI → Backend Service → Logs
# Должно быть:
✅ Database connected
✅ Telegram bot initialized
✅ Backend server running on port XXXX
```

### Frontend:
```bash
# В Railway UI → Frontend Service → Logs
# Должно быть:
✅ Ready on http://localhost:XXXX
```

## 📞 Если что-то не работает

1. **Проверьте Root Directory** - это самая частая ошибка!
2. **Проверьте логи** в Railway UI
3. **Убедитесь**, что все файлы закоммичены и запушены
4. **Проверьте переменные окружения**
5. **Убедитесь**, что порты открыты (Railway делает это автоматически)

