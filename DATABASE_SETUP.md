# Настройка PostgreSQL на Railway

## Проблема: ECONNREFUSED ::1:5432

Эта ошибка означает, что backend не может подключиться к PostgreSQL базе данных.

## Решение: Настройка DATABASE_URL

### Способ 1: Автоматическое подключение (Рекомендуется)

1. **Создайте PostgreSQL сервис:**
   - В Railway проекте нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
   - Railway автоматически создаст базу данных

2. **Подключите PostgreSQL к Backend сервису:**
   - Откройте ваш **Backend сервис** на Railway
   - Перейдите на вкладку **"Variables"**
   - Найдите секцию **"Reference Variable"** или **"Connect Service"**
   - Выберите ваш **PostgreSQL сервис**
   - Railway автоматически добавит `DATABASE_URL` переменную

3. **Проверьте переменную:**
   - В разделе Variables должна появиться `DATABASE_URL`
   - Значение будет автоматически подставлено из PostgreSQL сервиса

### Способ 2: Ручная настройка

1. **Получите DATABASE_URL из PostgreSQL сервиса:**
   - Откройте ваш **PostgreSQL сервис** на Railway
   - Перейдите на вкладку **"Variables"**
   - Найдите переменную `DATABASE_URL` или `POSTGRES_URL`
   - Скопируйте значение (обычно выглядит как: `postgresql://user:password@host:port/database`)

2. **Добавьте переменную в Backend сервис:**
   - Откройте ваш **Backend сервис** на Railway
   - Перейдите на вкладку **"Variables"**
   - Нажмите **"New Variable"**
   - Имя: `DATABASE_URL`
   - Значение: вставьте скопированный URL
   - Нажмите **"Add"**

3. **Перезапустите сервис:**
   - Railway автоматически перезапустит сервис после добавления переменной
   - Или нажмите **"Redeploy"** вручную

## Проверка настройки

После настройки проверьте логи Backend сервиса. Должно быть:

```
🔍 Checking environment variables...
   DATABASE_URL: ✅ Set
🔄 Attempting to connect to database... (1/5)
✅ Connected to PostgreSQL database
✅ Database connection verified
✅ Database tables initialized
```

## Если проблема сохраняется

### 1. Проверьте, что PostgreSQL сервис запущен
- В Railway должен быть активный PostgreSQL сервис
- Статус должен быть "Active" или "Running"

### 2. Проверьте формат DATABASE_URL
Правильный формат:
```
postgresql://username:password@hostname:port/database
```

Пример:
```
postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway
```

### 3. Проверьте SSL настройки
На Railway PostgreSQL обычно требует SSL. Код уже настроен на это:
```typescript
ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false
```

### 4. Проверьте, что сервисы в одном проекте
- Backend и PostgreSQL должны быть в одном Railway проекте
- Это необходимо для автоматического подключения

## Дополнительные переменные окружения

Убедитесь, что у Backend сервиса установлены:

- ✅ `DATABASE_URL` - подключение к PostgreSQL (обязательно)
- ✅ `TELEGRAM_BOT_TOKEN` - токен Telegram бота (опционально)
- ✅ `NODE_ENV=production` - режим работы (рекомендуется)
- ✅ `PORT` - Railway установит автоматически

## Автоматическое переподключение

Код теперь включает автоматические попытки переподключения:
- 5 попыток подключения
- Увеличивающаяся задержка между попытками
- Подробные логи для отладки

Если подключение не удается, проверьте логи для детальной информации.

