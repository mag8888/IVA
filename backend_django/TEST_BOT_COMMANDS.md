# Тестирование команд Telegram бота

## Текущий статус

✅ Команда `/start` работает - бот отвечает приветствием

## Команды для тестирования

### 1. `/start`
**Ожидаемый ответ:**
```
Привет, Roman! 👋

Твой реферальный код: [код]

Используй /app для просмотра структуры.
```

✅ Работает (видно на скриншоте)

### 2. `/app`
**Ожидаемое поведение:**
- Бот отправляет сообщение с кнопкой
- Кнопка: "🌳 Открыть мою структуру"
- При нажатии открывается мини-приложение

**Проверьте:**
1. Отправьте `/app` боту
2. Должна появиться кнопка
3. Нажмите на кнопку → откроется мини-приложение

### 3. `/stats`
**Ожидаемый ответ:**
- Статистика пользователя
- Уровень и позиция в структуре
- Бонусы (из БД)

## Если команды не работают

### Проверка на Railway:

1. **Проверьте логи Backend сервиса:**
   - Должно быть: `✅ Telegram бот запущен в отдельном потоке`
   - Должно быть: `🚀 Запуск Telegram бота (polling)...`

2. **Проверьте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` - должен быть установлен
   - `TELEGRAM_WEBAPP_URL` - должен быть установлен

3. **Перезапустите Backend:**
   - Нажмите "Redeploy" на Railway
   - Дождитесь завершения деплоя

### Проверка в коде:

Команды регистрируются в `integrations/telegram.py`:
- `CommandHandler("start", start_command)` ✅
- `CommandHandler("app", app_command)` ✅
- `CommandHandler("stats", stats_command)` ✅

## После исправления

После пуша изменений и деплоя на Railway:

1. Подождите 1-2 минуты после деплоя
2. Отправьте `/app` боту
3. Должна появиться кнопка с мини-приложением

## Если проблема сохраняется

Проверьте логи Railway на наличие ошибок:
- Ошибки подключения к Telegram API
- Ошибки инициализации бота
- Проблемы с токеном

