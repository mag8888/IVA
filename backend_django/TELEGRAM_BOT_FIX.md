# Исправление: Команды бота не подключены

## Проблема

Бот отвечает на `/start`, но не отвечает на `/app` и другие команды.

## Причина

Бот инициализировался, но не запускался (не вызывался `run_polling()`).

## Решение

✅ Исправлено: Бот теперь запускается в отдельном потоке при старте Django.

### Что изменено:

1. **`integrations/apps.py`**:
   - Добавлен запуск бота в отдельном потоке
   - Бот запускается автоматически при старте Django
   - Игнорируется при миграциях

2. **`integrations/telegram.py`**:
   - Добавлена функция `start_telegram_bot_async()` для запуска в потоке
   - Бот запускается с `run_polling()`

## Команды бота

Теперь все команды должны работать:

- ✅ `/start` - приветствие и реферальный код
- ✅ `/app` - открытие мини-приложения с MLM структурой
- ✅ `/stats` - статистика пользователя и бонусы

## Проверка работы

После деплоя на Railway проверьте логи Backend:

**Должно быть:**
```
Telegram бот инициализирован
✅ Telegram бот запущен в отдельном потоке
🚀 Запуск Telegram бота (polling)...
```

**Если есть ошибки:**
- Проверьте `TELEGRAM_BOT_TOKEN` в переменных окружения
- Проверьте, что токен правильный
- Проверьте логи на ошибки подключения

## Тестирование

1. Отправьте `/start` боту → должен ответить
2. Отправьте `/app` боту → должна появиться кнопка с мини-приложением
3. Отправьте `/stats` боту → должна показаться статистика

## Если команды все еще не работают

1. **Проверьте логи Railway** - должны быть сообщения о запуске бота
2. **Проверьте токен** - убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. **Перезапустите Backend** на Railway
4. **Проверьте, что бот не заблокирован** в Telegram

## Важно

- Бот запускается автоматически при старте Django
- Работает в отдельном потоке (не блокирует основной процесс)
- Все команды регистрируются при инициализации

