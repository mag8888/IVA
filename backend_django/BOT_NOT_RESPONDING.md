# Бот не отвечает - Диагностика и решение

## Быстрая проверка

### 1. Проверьте webhook через Telegram API

Замените `<YOUR_BOT_TOKEN>` на ваш токен:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

**Должно вернуть:**
```json
{
  "ok": true,
  "result": {
    "url": "https://iva-production.up.railway.app/telegram/webhook/",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "last_error_date": 0,
    "last_error_message": "",
    "max_connections": 40
  }
}
```

**Если `url` пустой или неправильный:**
- Webhook не установлен
- Нужно перезапустить Django сервис на Railway

**Если `last_error_message` не пустой:**
- Есть проблема с доступностью endpoint
- Проверьте логи Railway

### 2. Проверьте логи Railway

В логах Django сервиса должно быть:

**При старте:**
```
✅ Telegram бот инициализирован
🚀 Настройка Telegram бота через Webhook...
📡 Webhook URL: https://iva-production.up.railway.app/telegram/webhook/
✅ Webhook установлен: https://iva-production.up.railway.app/telegram/webhook/
✅ Telegram бот настроен через Webhook
```

**При получении сообщения:**
```
📨 Получено сообщение: /start от 123456789
✅ Webhook запрос принят, обработка запущена в потоке
🔄 Начинаю обработку обновления в потоке для пользователя 123456789
📥 Получена команда /start от пользователя 123456789
✅ Обновление успешно обработано для пользователя 123456789
```

### 3. Проверьте переменные окружения

В Railway Django сервисе должны быть установлены:

- ✅ `TELEGRAM_BOT_TOKEN` - токен бота
- ✅ `TELEGRAM_WEBHOOK_URL` или `RAILWAY_PUBLIC_DOMAIN` - для webhook
- ✅ `DATABASE_URL` - для подключения к БД

### 4. Проверьте доступность endpoint

Проверьте, что endpoint доступен:

```bash
curl -X POST https://iva-production.up.railway.app/telegram/webhook/ \
  -H "Content-Type: application/json" \
  -d '{"update_id": 1, "message": {"message_id": 1, "from": {"id": 123, "first_name": "Test"}, "chat": {"id": 123}, "date": 1234567890, "text": "/start"}}'
```

**Должно вернуть:**
```json
{"ok": true}
```

## Решения проблем

### Проблема 1: Webhook не установлен

**Симптомы:**
- `getWebhookInfo` возвращает пустой `url`
- В логах нет сообщения "✅ Webhook установлен"

**Решение:**
1. Проверьте переменные окружения:
   - `TELEGRAM_WEBHOOK_URL=https://iva-production.up.railway.app/telegram/webhook/`
   - Или `RAILWAY_PUBLIC_DOMAIN=iva-production.up.railway.app`
2. Перезапустите Django сервис на Railway
3. Проверьте логи - должно появиться "✅ Webhook установлен"

### Проблема 2: Endpoint недоступен

**Симптомы:**
- `getWebhookInfo` показывает `last_error_message` с ошибкой
- Ошибки 404, 500, timeout в логах

**Решение:**
1. Проверьте, что сервис запущен и доступен:
   ```bash
   curl https://iva-production.up.railway.app/health/
   ```
   Должно вернуть: `{"status": "ok", ...}`

2. Проверьте URL в `urls.py`:
   - Должен быть путь: `path('telegram/webhook/', telegram_webhook, name='telegram-webhook')`

3. Проверьте CORS настройки (если есть проблемы)

### Проблема 3: Бот получает обновления, но не отвечает

**Симптомы:**
- В логах есть "📨 Получено сообщение"
- Но нет "✅ Обновление успешно обработано"
- Или есть ошибки обработки

**Решение:**
1. Проверьте логи на ошибки:
   - Ищите строки с "❌"
   - Проверьте ошибки БД, импортов, и т.д.

2. Проверьте подключение к БД:
   - Убедитесь, что `DATABASE_URL` правильный
   - Проверьте, что миграции применены

3. Проверьте, что поле `telegram_id` существует в таблице `core_user`:
   ```sql
   SELECT column_name FROM information_schema.columns 
   WHERE table_name = 'core_user' AND column_name = 'telegram_id';
   ```
   
   Если колонки нет - нужно применить миграции:
   ```bash
   python manage.py migrate
   ```

### Проблема 4: Ошибка при создании пользователя

**Симптомы:**
- В логах: "❌ Ошибка создания пользователя"
- Пользователь не создается в БД

**Решение:**
1. Проверьте логи на конкретную ошибку
2. Возможные причины:
   - Проблемы с БД (нет подключения)
   - Ошибки валидации (дубликат username)
   - Проблемы с миграциями

### Проблема 5: Бот отвечает с задержкой или не отвечает вообще

**Симптомы:**
- Сообщения приходят, но ответа нет
- Или ответ приходит через несколько минут

**Решение:**
1. Проверьте логи - возможно обработка занимает много времени
2. Проверьте, нет ли блокирующих операций в обработчиках команд
3. Убедитесь, что обработка происходит в отдельном потоке (не блокирует ответ)

## Ручная проверка webhook

### Удалить webhook:
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook?drop_pending_updates=true"
```

### Установить webhook вручную:
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://iva-production.up.railway.app/telegram/webhook/", "drop_pending_updates": true}'
```

### Проверить статус:
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

## Контрольный список

- [ ] Webhook установлен и показывает правильный URL
- [ ] Endpoint `/telegram/webhook/` доступен и отвечает
- [ ] В логах есть "✅ Telegram бот настроен через Webhook"
- [ ] При отправке `/start` в логах появляются сообщения о получении и обработке
- [ ] Переменные окружения установлены правильно
- [ ] Миграции применены (поле `telegram_id` существует)
- [ ] База данных доступна и работает
- [ ] Нет ошибок в логах Railway

## Если ничего не помогает

1. **Перезапустите Django сервис** на Railway
2. **Удалите и заново установите webhook** (см. выше)
3. **Проверьте токен бота** - правильный ли он
4. **Проверьте логи Railway** на наличие ошибок
5. **Создайте тестовый запрос** к webhook endpoint вручную

