# Исправление ошибки 409 Conflict

## Проблема
```
error: [polling_error] {"code":"ETELEGRAM","message":"ETELEGRAM: 409 Conflict: terminated by other getUpdates request; make sure that only one bot instance is running"}
```

## Причина
Ошибка возникает, когда несколько экземпляров бота пытаются использовать один и тот же токен одновременно:
- Старый Node.js бот использует polling
- Новый Django бот пытается использовать webhook
- Или несколько экземпляров Django бота запущены одновременно

## Решение

### 1. Убедитесь, что старый Node.js бот отключен

**На Railway:**
1. Откройте сервис "IVA" (старый Node.js backend)
2. Перейдите в "Variables"
3. Удалите или переименуйте `TELEGRAM_BOT_TOKEN` (или установите `DISABLE_TELEGRAM_BOT=true`)
4. Перезапустите сервис

**Или полностью остановите старый сервис:**
- В Railway Dashboard → выберите старый Node.js сервис
- Settings → Delete Service (или просто остановите его)

### 2. Удалите старый webhook через Telegram API

Выполните на Railway (в Django сервисе):

```bash
python3 remove_webhook.py
```

Или через Telegram Bot API напрямую:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook?drop_pending_updates=true"
```

### 3. Проверьте переменные окружения Django сервиса

Убедитесь, что в Django сервисе установлены:
- `TELEGRAM_BOT_TOKEN` - токен бота
- `TELEGRAM_WEBHOOK_URL` - URL для webhook (например: `https://iva-production.up.railway.app/telegram/webhook/`)
- `RAILWAY_PUBLIC_DOMAIN` - публичный домен (например: `iva-production.up.railway.app`)

### 4. Перезапустите Django сервис

После изменений перезапустите сервис на Railway.

### 5. Проверьте логи

После перезапуска в логах должно быть:
```
✅ Telegram бот инициализирован
✅ Webhook установлен: https://iva-production.up.railway.app/telegram/webhook/
✅ Telegram бот настроен через Webhook
```

**НЕ должно быть:**
- `🚀 Запуск Telegram бота (polling)...`
- `polling_error`
- `409 Conflict`

## Проверка работы

1. Отправьте `/start` боту в Telegram
2. Бот должен ответить без ошибок
3. Проверьте логи Railway - не должно быть ошибок 409

## Если проблема сохраняется

1. **Проверьте, не запущены ли оба сервиса одновременно:**
   - Старый Node.js сервис (должен быть остановлен или без TELEGRAM_BOT_TOKEN)
   - Новый Django сервис (должен иметь TELEGRAM_BOT_TOKEN и TELEGRAM_WEBHOOK_URL)

2. **Удалите webhook вручную:**
   ```bash
   curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook?drop_pending_updates=true"
   ```

3. **Подождите 1-2 минуты** и перезапустите Django сервис

4. **Проверьте, что webhook установлен правильно:**
   ```bash
   curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
   ```

   Должно вернуть:
   ```json
   {
     "ok": true,
     "result": {
       "url": "https://iva-production.up.railway.app/telegram/webhook/",
       "has_custom_certificate": false,
       "pending_update_count": 0
     }
   }
   ```

