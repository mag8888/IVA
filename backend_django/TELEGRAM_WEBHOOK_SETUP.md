# Настройка Telegram Webhook

## Что изменилось

✅ Telegram бот теперь использует **Webhook** вместо Polling. Это решает проблему 409 Conflict и работает лучше в production.

## Как работает Webhook

1. **Telegram отправляет обновления на наш сервер** (вместо того, чтобы мы опрашивали Telegram)
2. **Нет конфликтов** - только один endpoint получает обновления
3. **Быстрее** - обновления приходят мгновенно
4. **Надежнее** - нет проблем с множественными процессами

## Настройка на Railway

### Вариант 1: Автоматическая настройка (рекомендуется)

Если у вас установлена переменная `RAILWAY_PUBLIC_DOMAIN`, webhook настроится автоматически:

1. **Добавьте переменную окружения на Railway:**
   - `RAILWAY_PUBLIC_DOMAIN` = ваш домен Railway (например, `iva-production.up.railway.app`)
   - Или Railway автоматически установит эту переменную

2. **Webhook URL будет автоматически:** `https://ваш-домен/telegram/webhook/`

### Вариант 2: Ручная настройка

Если хотите указать URL вручную:

1. **Добавьте переменную окружения на Railway:**
   - `TELEGRAM_WEBHOOK_URL` = `https://ваш-домен.railway.app/telegram/webhook/`
   - Обязательно используйте HTTPS!

## Проверка работы

После деплоя проверьте логи Backend:

**Должно быть:**
```
✅ Telegram бот инициализирован
🚀 Настройка Telegram бота через Webhook...
📡 Webhook URL: https://ваш-домен/telegram/webhook/
🗑️  Удален предыдущий webhook
✅ Webhook установлен: https://ваш-домен/telegram/webhook/
📡 Webhook info: https://ваш-домен/telegram/webhook/, pending updates: 0
✅ Telegram бот настроен через Webhook
```

**Не должно быть:**
- Ошибок 409 Conflict
- Ошибок подключения

## Тестирование

1. **Отправьте `/start` боту** → должен ответить
2. **Отправьте `/app` боту** → должна появиться кнопка
3. **Отправьте `/stats` боту** → должна показаться статистика

## Проверка webhook через API

Вы можете проверить статус webhook через Telegram Bot API:

```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

Ответ должен содержать:
```json
{
  "ok": true,
  "result": {
    "url": "https://ваш-домен/telegram/webhook/",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

## Устранение проблем

### Проблема: Webhook не устанавливается

**Решение:**
1. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен
2. Проверьте, что `RAILWAY_PUBLIC_DOMAIN` или `TELEGRAM_WEBHOOK_URL` установлен
3. Убедитесь, что URL начинается с `https://`
4. Проверьте логи на ошибки

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи на ошибки обработки webhook
2. Убедитесь, что endpoint `/telegram/webhook/` доступен
3. Проверьте статус webhook через API (см. выше)

### Проблема: Множественные обновления

**Решение:**
- Webhook автоматически удаляет предыдущие обновления при установке
- Если проблема сохраняется, перезапустите Backend на Railway

## Отключение Webhook

Если нужно вернуться к Polling (не рекомендуется):

1. Добавьте переменную окружения: `DISABLE_TELEGRAM_BOT=true`
2. Или удалите `TELEGRAM_WEBHOOK_URL` и `RAILWAY_PUBLIC_DOMAIN`

## Преимущества Webhook

✅ **Нет конфликтов** - только один endpoint получает обновления  
✅ **Быстрее** - обновления приходят мгновенно  
✅ **Надежнее** - нет проблем с множественными процессами  
✅ **Масштабируемость** - можно использовать load balancer  
✅ **Production-ready** - рекомендованный способ для production

## Дополнительная информация

- [Telegram Bot API - Webhooks](https://core.telegram.org/bots/api#setwebhook)
- [python-telegram-bot - Webhooks](https://docs.python-telegram-bot.org/en/stable/telegram.bot.html#telegram.Bot.set_webhook)

