# Проверка переменных окружения на Railway

## Текущие переменные (из скриншота)

✅ `DATABASE_URL` - подключение к PostgreSQL (скрыт)
✅ `TELEGRAM_BOT_TOKEN` - токен бота (скрыт)
✅ `TELEGRAM_WEBAPP_URL` = `iva-production.up.railway.app`

## Рекомендации

### TELEGRAM_WEBAPP_URL

**Текущее значение**: `iva-production.up.railway.app`

**Рекомендуется**: Добавить `https://` в начало

**Правильное значение**:
```
https://iva-production.up.railway.app
```

**Почему**: 
- Код автоматически добавляет `https://` если его нет
- Но лучше указать явно для надежности
- Telegram требует HTTPS для Web Apps

### Дополнительные переменные (опционально)

Для полной функциональности можно добавить:

```
MAX_PARTNERS_PER_LEVEL=3
DEFAULT_GREEN_BONUS_PERCENT=50
DEFAULT_YELLOW_BONUS_PERCENT=50
DJANGO_DEBUG=False
NODE_ENV=production
```

## Проверка работы

После настройки переменных:

1. **Перезапустите Backend сервис** на Railway
2. **Проверьте логи** - должно быть:
   ```
   ✅ Telegram bot initialized
   Telegram bot is running...
   ```
3. **Протестируйте команду `/app`** в боте:
   - Отправьте `/app` боту
   - Должна появиться кнопка "🌳 Открыть мою структуру"
   - Нажмите на кнопку → откроется мини-приложение

## Если мини-приложение не открывается

1. Проверьте, что `TELEGRAM_WEBAPP_URL` правильный
2. Убедитесь, что URL начинается с `https://`
3. Проверьте, что Backend доступен по этому URL
4. Проверьте логи Backend на ошибки

## Итого

✅ **DATABASE_URL** - правильно настроен
✅ **TELEGRAM_BOT_TOKEN** - правильно настроен
⚠️ **TELEGRAM_WEBAPP_URL** - лучше добавить `https://` в начало

**Рекомендуемое значение**:
```
TELEGRAM_WEBAPP_URL=https://iva-production.up.railway.app
```

