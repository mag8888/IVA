# Пошаговая инструкция: Добавить DATABASE_URL в Backend

## Что вы видите:

В PostgreSQL сервисе есть:
- `DATABASE_PUBLIC_URL` - публичный URL (виден в скриншоте)
- `DATABASE_URL` - основной URL (скрыт звездочками)

## Что нужно сделать:

### Вариант 1: Использовать DATABASE_URL (Рекомендуется) ⭐

1. **В PostgreSQL сервисе:**
   - Найдите переменную `DATABASE_URL` (та, что со звездочками)
   - Нажмите на значок **глаза** 👁️ или **копирования** 📋
   - Скопируйте значение

2. **Откройте Backend сервис "IVA":**
   - Перейдите на вкладку **"Variables"**
   - Нажмите **"+ New"** или **"New Variable"**

3. **Добавьте переменную:**
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение из `DATABASE_URL`
   - Нажмите **"Add"**

### Вариант 2: Использовать DATABASE_PUBLIC_URL (Если DATABASE_URL недоступна)

Если не можете скопировать `DATABASE_URL`, используйте `DATABASE_PUBLIC_URL`:

1. **В PostgreSQL сервисе:**
   - Скопируйте значение `DATABASE_PUBLIC_URL`:
     ```
     postgresql://postgres:FiIqSkIiXDGQaDueSWpNKewrkwdoPKpd@mainline.proxy.rlwy.net:25155/railway
     ```

2. **Откройте Backend сервис "IVA":**
   - Перейдите на вкладку **"Variables"**
   - Нажмите **"+ New"**

3. **Добавьте переменную:**
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение из `DATABASE_PUBLIC_URL`
   - Нажмите **"Add"**

## Важно!

- ✅ Имя переменной должно быть именно `DATABASE_URL` (не `DATABASE_PUBLIC_URL`)
- ✅ Значение можно взять из `DATABASE_PUBLIC_URL` или `DATABASE_URL`
- ✅ После добавления Railway автоматически перезапустит сервис

## Проверка:

После добавления проверьте логи Backend сервиса "IVA". Должно быть:

```
🔍 Checking environment variables...
   DATABASE_URL: ✅ Set
🔄 Attempting to connect to database...
✅ Connected to PostgreSQL database
```

## Если используете Reference Variable (лучший способ):

1. В Backend сервисе "IVA" → Variables
2. Нажмите "+ New" → "Reference Variable"
3. Service: выберите "Postgres"
4. Variable: выберите "DATABASE_URL"
5. Railway автоматически подключит переменную

Этот способ лучше, потому что Railway будет автоматически обновлять значение!

