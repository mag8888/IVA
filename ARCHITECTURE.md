# Архитектура проекта

## 🔄 Схема подключений

```
┌─────────────┐         HTTP API          ┌─────────────┐
│  Frontend   │ ────────────────────────> │   Backend   │
│  (Next.js)  │ <──────────────────────── │  (Express)  │
└─────────────┘                            └──────┬──────┘
                                                  │
                                                  │ PostgreSQL
                                                  │ Connection
                                                  │
                                         ┌────────▼────────┐
                                         │   PostgreSQL    │
                                         │    Database     │
                                         └─────────────────┘
```

## 📍 Где что подключается

### ✅ Backend подключается к:
- **PostgreSQL база данных** - напрямую через `DATABASE_URL`
- **Telegram Bot API** - для работы бота

### ✅ Frontend подключается к:
- **Backend API** - через HTTP запросы (axios)
- **НЕ подключается к базе данных напрямую** ❌

## 🔐 Почему так?

### Безопасность
- База данных не должна быть доступна из браузера
- Все запросы к БД проходят через бэкенд с валидацией
- Бэкенд контролирует доступ к данным

### Разделение ответственности
- **Backend**: работа с данными, бизнес-логика, безопасность
- **Frontend**: отображение данных, пользовательский интерфейс

### Масштабируемость
- Можно иметь несколько фронтендов (веб, мобильное приложение)
- Все они используют один бэкенд API
- Бэкенд может кэшировать данные, оптимизировать запросы

## 🔌 Настройка на Railway

### Backend сервис:
```
✅ DATABASE_URL - подключение к PostgreSQL
✅ TELEGRAM_BOT_TOKEN - токен Telegram бота
✅ PORT - порт сервера (автоматически)
✅ NODE_ENV=production
```

### Frontend сервис:
```
✅ NEXT_PUBLIC_API_URL - URL бэкенд сервера
   Например: https://your-backend.railway.app
✅ NODE_ENV=production
❌ НЕ нужен DATABASE_URL
```

## 📡 Как работает общение

### Пример запроса:

1. **Пользователь открывает фронтенд** → видит интерфейс
2. **Фронтенд делает запрос:**
   ```javascript
   axios.get('https://backend.railway.app/api/users')
   ```
3. **Бэкенд получает запрос** → обращается к базе данных
4. **Бэкенд получает данные из БД** → обрабатывает их
5. **Бэкенд отправляет ответ** → JSON данные
6. **Фронтенд получает данные** → отображает в интерфейсе

### Поток данных:

```
User → Frontend → Backend API → PostgreSQL
                ← JSON Response ← Database Result
```

## 🛡️ Безопасность

### ❌ НЕПРАВИЛЬНО (не делайте так):
```
Frontend → PostgreSQL (напрямую)
```
Проблемы:
- Утечка credentials в браузер
- Нет контроля доступа
- SQL инъекции возможны

### ✅ ПРАВИЛЬНО (как сейчас):
```
Frontend → Backend API → PostgreSQL
```
Преимущества:
- Credentials только на сервере
- Валидация всех запросов
- Контроль доступа
- Безопасность

## 📝 Переменные окружения

### Backend (.env):
```env
DATABASE_URL=postgresql://...  # ✅ Подключение к БД
TELEGRAM_BOT_TOKEN=...         # ✅ Токен бота
PORT=3001                      # ✅ Порт сервера
```

### Frontend (.env.local):
```env
NEXT_PUBLIC_API_URL=http://localhost:3001  # ✅ URL бэкенда
# DATABASE_URL НЕ НУЖЕН! ❌
```

## 🎯 Итого

- **База данных** → подключается только к **Backend**
- **Frontend** → подключается только к **Backend API**
- **Backend** → мост между Frontend и Database

Это стандартная и правильная архитектура для веб-приложений! ✅

